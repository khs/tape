name: Refresh market data

# Weekly on Sunday at 06:00 UTC (~1-2 AM ET depending on DST), plus manual
# trigger from the GitHub Actions UI for ad-hoc refreshes.
on:
  schedule:
    - cron: '0 6 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install yfinance pandas

      # Pipelines are run independently with continue-on-error so one failing
      # provider (Yahoo rate-limiting us, FRED rejecting a series id, etc.)
      # doesn't block the rest. Ordering matters: countries_relative reads VT
      # + country-ETF data written by yahoo_quotes.

      - name: Refresh FRED series
        continue-on-error: true
        run: python pipelines/fred_series.py

      - name: Refresh Yahoo quotes (equities, ETFs, futures)
        continue-on-error: true
        run: python pipelines/yahoo_quotes.py

      - name: Refresh Yahoo market caps (tech mega-caps)
        continue-on-error: true
        run: python pipelines/yahoo_marketcap.py

      - name: Compute countries-relative (country ETF / VT)
        continue-on-error: true
        run: python pipelines/countries_relative.py

      - name: Refresh World Bank real-GDP shares
        continue-on-error: true
        run: python pipelines/worldbank_gdp.py

      - name: Refresh BLS subseries (CPI components, JOLTS, state unemployment)
        continue-on-error: true
        run: python pipelines/bls.py

      # CBO + OECD are stubs — see their module docstrings for the build
      # plan. They no-op cleanly so flipping them on later is just a
      # matter of filling in the body.
      - name: Refresh CBO projections (stub)
        continue-on-error: true
        run: python pipelines/cbo.py

      - name: Refresh OECD cross-country comparisons (stub)
        continue-on-error: true
        run: python pipelines/oecd.py

      # After all pipelines have written their fresh JSON, copy the whole
      # data/ tree into a dated archive at data/_archive/<YYYY-MM-DD>/...
      # so cited URLs can resolve a specific data vintage indefinitely.
      # Storage is cheap; revisions and provider restatements are not
      # reversible without this.
      - name: Snapshot data for citation integrity
        run: python pipelines/snapshot_data.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data/
          if git diff --cached --quiet; then
            echo "No data changes to commit."
          else
            git commit -m "chore: weekly data refresh"
            git push
          fi
