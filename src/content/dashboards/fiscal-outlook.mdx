---
title: "Fiscal outlook"
order: 4
defaultDelta: 5y
description: "Forward-looking views of fiscal and energy series, with the projection segments drawn as dashed extensions of the historical line. Currently scoped to the three energy commodity futures curves; CBO and SSA projection sections are queued behind a data-ingest step (see About below)."
aboutBlurb: "Forward-looking projections from the institutions that publish them — futures markets for energy commodities, CBO for the federal budget baseline, SSA for the long-run Social Security trajectory. The renderer draws projection segments as dashed extensions of the historical line so the past-vs-forecast boundary is visible at a glance."
sections:
  - title: "CBO budget baseline — 10-year projections"
    description: "Each chart's solid line is historical actuals from CBO's Budget Outlook data appendices; the dashed extension is the most recent 10-year baseline projection. The baseline holds current law fixed — actual outcomes typically diverge as expiring provisions get extended. Historical coverage is sparse (one actual year per Outlook release on file); a 1962-onward continuous line will land if we add CBO's Historical Budget Data file."
    charts:
      - government/cbo_outlays_pct_gdp
      - government/cbo_revenues_pct_gdp
      - government/cbo_deficit_pct_gdp
      - government/cbo_debt_pct_gdp
  - title: "Energy commodities — current forward curves"
    description: "Solid lines are monthly-averaged historical spot prices. Dashed extensions are today's futures curves — each future-month contract plotted at its delivery month, priced at its most-recent close. These are market-quoted contract prices, not Tape-generated forecasts."
    charts:
      - energy/wti_curve
      - energy/brent_curve
      - energy/natgas_curve
---

## What's on this dashboard

The Forecast UI ships forward-looking projections from the institutions
that publish them — not Tape-generated forecasts. In every chart, the
solid line is historical actuals and the dashed line is the projection.
The renderer extends the x-axis automatically to fit the projection's
last point.

The scope today is the three energy commodity futures curves — WTI
crude, Brent crude, and Henry Hub natural gas. Each refreshes on every
pipeline run with the latest NYMEX-quoted contract prices.

## What's queued

One more section will land once its data files are ingested:

- **SSA Trustees Report — 75-year intermediate-cost projection** for
  OASDI cost / GDP and covered workers per beneficiary. The Trustees
  Report publishes annually each May/June.

The SSA pipeline (`pipelines/ssa.py`) is written and committed but
ssa.gov blocks direct CI fetches with a 403. The pipeline falls back
to operator-supplied HTML files under `pipelines/ssa_data/`; once
those land on disk, running the pipeline will populate the data files
and the chart YAMLs + dashboard section can be re-added.

## How to read a futures curve

A futures curve isn't a forecast in the same sense as a CBO baseline
or an SSA projection. It's the actual price at which market
participants are willing to buy and sell contracts that deliver the
commodity in each future month. So it represents:

- **A consensus of market participants' expected prices**, weighted
  by who is willing to put money behind each view.
- **A risk-adjusted view** — buyers value certainty, sellers value
  storage costs and convenience yield, so the curve incorporates
  more than pure expectation.
- **An option to act**: anyone who disagrees with the curve can
  trade against it, which is why persistent curve mispricings tend
  to be eaten.

For oil specifically, the curve is the cleanest public price-forecast
available. It's not always right (1- to 4-month errors average $8-10
per barrel in WTI backtests), but it's the best price-discovery
mechanism we have.

## Customizing this dashboard

Every source on this dashboard is in the composer library. The
"Customize" link at the top opens this dashboard pre-loaded in the
composer so you can swap in alternative commodity series, add your
own overlays, or build a custom forecast stack.

The vintage picker (Phase 3 of the Forecast UI) is not yet shipped —
when it lands, you'll be able to compare an older snapshot of the
futures curve to today's. The yahoo_futures pipeline already
accumulates per-run snapshots, so when the picker ships it'll pick
up the back-data automatically.
